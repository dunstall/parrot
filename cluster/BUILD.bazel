cc_library(
    name = "cluster",
    srcs = glob(
        ["*.cc"],
        exclude = ["*_test.cc"],
    ),
    hdrs = glob(["*.h"]),  # TODO only expose cluster.h
    visibility = ["//visibility:public"],
    deps = [
        "//api:cluster_cc_grpc",
        "//include",
        "//store",
        "@com_github_grpc_grpc//:grpc++",
    ],
)
