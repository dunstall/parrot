cc_test(
    name = "cluster-test",
    srcs = glob(["*_test.cc"]),
    copts = [
        "-Iexternal/gtest/include",
    ],
    deps = [
        "//cluster",
        "@com_github_grpc_grpc//:grpc++",
        "@gtest//:gtest_main",
    ],
)
